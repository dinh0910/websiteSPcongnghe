@model IEnumerable<websiteSPcongnghe.Models.Quyenhan>

@{
    ViewData["Title"] = "Index";
}

<div class="my-5">
    <a asp-action="Create" class="btn btn-secondary btn-icon-split">
        <span class="icon text-white-50">
            <i class="fas fa-arrow-right"></i>
        </span>
        <span>Thêm quyền hạn</span>
    </a>
</div>

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Danh sách Quyền hạn</h6>
    </div>
    <div class="card-body">
        <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        Quyền hạn
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Quyen)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.QuyenhanID">Edit</a> |
                            <a asp-action="Delete" asp-route-id="@item.QuyenhanID">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
